---
name: status
description: Show skills-sync configuration and sync status
---

Show the current skills-sync configuration, GitHub authentication status, skill counts, and last sync times.

Run the status script:

```bash
bash "${CLAUDE_PLUGIN_ROOT}/scripts/status.sh"
```

Present the output to the user in a clear, organized format.
