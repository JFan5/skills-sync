---
name: push-plugins
description: Push installed plugins list to GitHub for cross-device sync
---

Back up your installed Claude Code plugins and marketplace sources to GitHub.

Run the push-plugins script:

```bash
bash "${CLAUDE_PLUGIN_ROOT}/scripts/push-plugins.sh"
```

Show the user what was uploaded (plugin names and marketplace sources).
